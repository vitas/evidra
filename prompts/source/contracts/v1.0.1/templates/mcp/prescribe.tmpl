# contract: {{ .ContractVersion }}

{{ .Contract.MCP.Prescribe.Intro }}

Required inputs:
{{ range .Contract.MCP.Prescribe.RequiredInputs -}}
- {{ . }}
{{ end }}

Recommended actor fields for behavior slicing:
{{ range .Contract.MCP.Prescribe.RecommendedActorFields -}}
- {{ . }}
{{ end }}

Optional correlation inputs:
{{ range .Contract.MCP.Prescribe.OptionalCorrelationInputs -}}
- {{ . }}
{{ end }}

Pre-call checklist:
{{ range .Contract.MCP.Prescribe.PreCallChecklist -}}
- {{ . }}
{{ end }}

When to call:
- Must call for mutate operations (apply/delete/patch/create/replace/upgrade/destroy/import/rollback).
- Skip for read-only operations (get/describe/logs/plan/show/status/template).
- If unsure, call prescribe.

Returns:
{{ range .Contract.MCP.Prescribe.Returns -}}
- {{ . }}
{{ end }}

Interpretation:
- `effective_risk` is the summarized decision hint.
- `risk_inputs` contains the per-source risk observations behind that summary.

{{ .Contract.MCP.Prescribe.FailureRule }}
