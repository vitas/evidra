apiVersion: apps/v1
kind: Deployment
metadata:
  name: broken-stand-deploy
  namespace: workloads-dev
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: broken-stand-deploy
    spec:
      containers:
        - name: nginx
          image: nginx:1.27-alpine
