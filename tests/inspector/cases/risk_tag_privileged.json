{
  "id": "risk_tag_privileged",
  "steps": [
    {
      "tool": "prescribe_full",
      "input": {
        "actor": {"type": "agent", "id": "risk-agent", "origin": "mcp"},
        "tool": "kubectl",
        "operation": "apply",
        "artifact_file": "tests/inspector/fixtures/privileged-pod.yaml"
      },
      "assert": {
        "ok": true,
        "has_fields": ["prescription_id", "artifact_digest", "intent_digest"]
      }
    }
  ]
}
