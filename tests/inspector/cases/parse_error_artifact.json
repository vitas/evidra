{
  "id": "raw_artifact_intent_only",
  "steps": [
    {
      "tool": "prescribe_full",
      "input": {
        "actor": {"type": "agent", "id": "parse-agent", "origin": "mcp"},
        "tool": "terraform",
        "operation": "apply",
        "raw_artifact": "not valid json {{{"
      },
      "assert": {
        "ok": true,
        "has_fields": ["prescription_id", "artifact_digest", "intent_digest"]
      }
    }
  ]
}
